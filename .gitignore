# --- Секреты (главное!) ----------------------------------------------------
# Любые env-файлы — никогда в репозиторий. Даже .txt-копии (Windows иногда
# создаёт .env.txt чтобы открыть в блокноте — пусть тоже ignorируется).
.env
.env.*
*.env
*.env.txt
*.env.local.txt
# Локальные дампы реальных секретов под Telegram-интеграцию (Сессия 39+).
# Файл создаёт Влад руками, токены ботов и chat_id — никогда в репозиторий.
telegram.txt
TELEGRAM.txt
# Допускаем только примеры без реальных ключей:
!.env.example
!**/.env.example

# --- Зависимости и сборки ---------------------------------------------------
node_modules/
.next/
out/
dist/
build/
.vercel/
.turbo/

# Кэш TypeScript
*.tsbuildinfo
next-env.d.ts

# --- Логи -------------------------------------------------------------------
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- IDE и OS ---------------------------------------------------------------
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo

# --- Прочее -----------------------------------------------------------------
# Локальные настройки Claude Code (не код проекта)
.claude/
# Временные файлы и бэкапы
*.tmp
*.bak

# --- Референсы для портирования --------------------------------------------
# Локальные копии Python-файлов из ДК Лурье — нужны Claude Code как образец
# при портировании сервисов команды на Node.js. В Git не попадают.
_reference_dkl/

# --- Supabase CLI -----------------------------------------------------------
# Локальный кеш `supabase` CLI — токены сессии, snapshot'ы линка. В Git не нужен.
supabase/.temp/

# --- Playwright MCP --------------------------------------------------------
# Снимки/логи Playwright MCP (snapshots, screenshots, console). В Git не нужны.
.playwright-mcp/
