# ПОТОК — Суперап

## Общее описание
Веб-приложение "Поток" — прод: https://potok-omega.vercel.app (бэкенд: https://potok-production-a2dc.up.railway.app), позже — собственный домен. Личный суперап с инструментами для жизни и работы. Владелец — Влад, радиожурналист, запускает блог в Инстаграме. Опыт разработки — нулевой, вся разработка ведётся через Claude Code.

## Навигация суперапа
Боковое меню auto-hide: скрыто по умолчанию, появляется при наведении мыши к левому краю экрана (на мобилке — бургер). Структура:
- **Блог**
  - **Инструменты** → **База референсов** (бывший "Аналитик", текущий проект)
  - **Команда** (будущее: AI-агенты — аналитик, редактор, креатор)
В будущем добавятся другие разделы суперапа: Финансы, Новости, Сохранёнки и другие.
Меню расширяемо — добавление нового раздела не требует переделки.

---

## ТЕКУЩИЙ ПРОЕКТ: Блог → Инструменты → База референсов

### Суть
Инструмент для анализа сохранённых Reels из Инстаграма (ранее назывался "Аналитик"). Влад вставляет ссылку (или список ссылок) на видео → система автоматически собирает данные, транскрибирует, анализирует через AI и складывает в таблицу с фильтрацией. Цель — разобрать тысячи сохранённых видео, систематизировать и найти идеи для собственного блога.

### Пользовательский сценарий
1. Открываю сайт → в левом меню нажимаю "Блог" → "Аналитик"
2. Нажимаю "Добавить видео" → вставляю ссылку на Reels → нажимаю "Анализировать"
3. Система обрабатывает (30–60 сек на одно видео): парсит данные, транскрибирует, прогоняет через AI
4. В таблице появляется новая строка со всеми данными
5. Я просматриваю, ставлю теги, пишу заметки, фильтрую, удаляю мусор
6. Массовое добавление: загружаю список ссылок (до 200–300 за батч) → фоновая обработка с прогресс-баром → можно закрыть браузер, результаты появляются по мере готовности

### Структура данных (столбцы таблицы)

#### Блок 1 — Справочная информация (заполняется автоматически)
- Дата публикации видео
- Ссылка на видео
- Автор (название блога)
- Ссылка на профиль автора
- Описание под видео (caption)
- Превью/обложка (миниатюра)

#### Блок 2 — Статистика (заполняется автоматически)
- Просмотры
- Лайки
- Комментарии
- ~~Шеры (поделились)~~ — Instagram не отдаёт публично с 2023, столбец убран из UI (см. «Известные ограничения»)
- Коэффициент вирусности (расчётный, формула на основе просмотров/лайков/комментов относительно средних показателей аккаунта)

#### Блок 3 — Содержание (заполняется автоматически)
- AI-саммари: краткое описание о чём видео (2–3 предложения)
- Транскрипция: полный текст речи из видео. Если речи нет — значение "Без речи (музыка/визуал)"
- Категория AI: автоматическая категоризация. Стартовый набор категорий (AI может предложить новую если ничего не подходит):
  - Вайб-кодинг
  - История
  - Культура
  - Санкт-Петербург
  - Юмор
  - Лайфстайл
  - Бизнес / предпринимательство
  - Путешествия
  - Еда
  - Мотивация / саморазвитие
  - Технологии
  - Образование
  - Другое (с указанием предложенной категории)

#### Блок 4 — Ручные поля (заполняются пользователем)
- Категория Я: пользователь подтверждает категорию AI или ставит свою
- Теги (множественный выбор, можно ставить несколько, Notion-стиль с цветами):
  - 🎯 Тема — хочу сделать похожее по теме
  - 🪝 Хук — цепляющее начало, разобрать приём
  - 🎨 Визуал — красиво снято/смонтировано, повторить
  - ⭐ Автор — интересный блогер, следить
  - 🗑 Мусор — пометить для удаления
  - (пользователь может создавать новые теги в процессе работы)
- Заметка: свободное текстовое поле, необязательное (поддерживает голосовой ввод через микрофон)
- Оценка (эмодзи рядом с превью):
  - ✅ Верифицировано — видео просмотрено и подтверждено как полезное
  - 🔥 Супер — выдающееся видео, вернуться первым
  - 🔄 Повторить — снять такое же видео слово в слово

### Таблица закладок (bookmarks)
Отдельная таблица в Supabase для видео, которые не относятся к блогу, но которые Влад не хочет терять (рестораны, выставки, AI-контент и т.д.). Структура — копия основных полей из videos (url, author, description, thumbnail, ai_summary, ai_category, user_note). Наполняется из Tinder-режима (свайп влево → "В закладки"). На сайте пока не отображается — доступна через админку Supabase. В будущем станет фундаментом для раздела "Сохранёнки".

### Функциональность таблицы
- Дата публикации = гиперссылка на видео в Instagram (открывается в новой вкладке)
- Превью: клик = увеличение в модалке (лупа)
- Фильтрация по: категории AI, категории Я, тегам, автору, наличию/отсутствию транскрипции, оценке
- Сортировка по: дате, просмотрам, лайкам, коэффициенту вирусности
- Поиск по тексту (в транскрипции, саммари, описании, заметках)
- Удаление: единичное и массовое (чекбоксы + "Выбрать все" + "Удалить выбранные" с подтверждением)
- Фильтр "Мусор" + удалить все отфильтрованные одним действием
- Режим просмотра (Tinder-swipe) — кнопка "Режим просмотра" запускает полноэкранный просмотр с учётом текущих фильтров

### Системный промпт для AI-анализа
При обработке видео AI получает: описание (caption), транскрипцию (если есть), статистику. На выходе:
- Саммари (2–3 предложения, о чём видео)
- Категория из предложенного списка (или новая с пояснением)
Промпт должен быть на русском. Если транскрипция пустая — AI работает только по описанию и обложке.

---

## Технический стек

### Фронтенд
- **Next.js** (React) — на Vercel
- **Tailwind CSS** — стилизация
- Адаптивный дизайн: работает и на десктопе, и на смартфоне

### Бэкенд
- **Node.js** на **Railway** — фоновая обработка видео (очередь задач)
- REST API между фронтом и бэкендом

### База данных
- **Supabase** (PostgreSQL) — хранение всех данных

### Внешние сервисы
- **Apify** (Instagram Reels Scraper) — парсинг данных и статистики из Reels
- **OpenAI API** — Whisper для транскрипции + GPT для саммари/категоризации (один ключ на оба назначения)

### Переменные окружения (.env)
```
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
OPENAI_API_KEY=
APIFY_API_TOKEN=
RAILWAY_BACKEND_URL=
```

---

## Структура папок
```
superapp/
  potok/                    ← монорепо проекта
    frontend/               ← Next.js приложение (деплоится на Vercel)
      src/
        app/                ← страницы (App Router)
        components/         ← React-компоненты
          layout/           ← боковое меню, навигация
          blog/             ← компоненты раздела "Блог"
            analyst/        ← компоненты Аналитика
        lib/                ← утилиты, API-клиенты, хелперы
        styles/             ← глобальные стили
      public/               ← статика
    backend/                ← Node.js/Express (деплоится на Railway)
      src/
        index.js            ← точка входа
        app.js              ← настройка express/CORS/роутов
        config/             ← валидация env
        routes/             ← API-эндпоинты
        services/           ← бизнес-логика (apify, supabase, orchestrator)
        queue/              ← зарезервировано под сессию 9 (массовое добавление)
    supabase/
      migrations/           ← SQL-миграции (запускаются вручную в SQL Editor)
    CLAUDE.md               ← этот файл
```

---

## Дизайн — КРИТИЧЕСКИ ВАЖНО

### Общий подход
Сайт должен выглядеть профессионально и современно. НЕ как типичный vibe-coding проект.

### Конкретные требования
- **Шрифты**: НЕ Inter, НЕ Roboto, НЕ системные. Использовать характерные, качественные шрифты. Для заголовков — один выразительный шрифт, для текста — другой, читабельный. Подключать через Google Fonts.
- **Размеры элементов**: Крупные, удобные кнопки и элементы навигации. Никаких мелких кнопок. Clickable area минимум 44x44px. Текст — не мельче 14px, основной — 16px.
- **Цветовая схема**: Светлая тема. Не белый-на-белом — должна быть глубина и контраст. Акцентный цвет — один, яркий, используется точечно.
- **Пространство**: Щедрые отступы. Воздух между элементами. Таблица не должна выглядеть как Excel — строки с паддингами, hover-эффекты, читаемость.
- **Иконки**: Lucide React. Не перегружать, но использовать где улучшают навигацию.
- **Анимации**: Минимальные, но присутствуют — плавные переходы страниц, hover-эффекты, появление элементов.
- **Мобильная версия**: Полноценная, не урезанная. Меню — бургер. Таблица — карточки на мобильном.

### Эстетика
Ориентир — Notion, Linear, Raycast. Чистый, уверенный дизайн с характером. Не generic, не bootstrap-like.

---

## Задачи для разработки (по сессиям)

### ✅ Готово
**Сессия 1: Инфраструктура + каркас приложения** (завершена 2026-04-22)
- Структура папок `frontend/` и `backend/` создана
- Next.js 15 (App Router) + React 19 + TypeScript + Tailwind настроены
- Шрифты: Unbounded (заголовки) + Manrope (текст), оба с кириллицей, через next/font
- Цветовая схема: тёплый off-white (`#F7F5F0`) + оранжевый акцент (`#FF5B1F`)
- Layout: боковое меню слева (desktop) / бургер-меню (мобильный), основная область справа
- Меню расширяемо — новые разделы добавляются через массив `navSections` в `frontend/src/lib/nav.ts`
- Страница Аналитика: заголовок + описание + кнопка "Добавить видео" (disabled до сессии 3) + empty state
- Типы данных `Video` вынесены в `frontend/src/lib/types.ts`
- Бэкенд: заглушка, реальный сервер — сессия 4

**Сессия 2: База данных** (завершена 2026-04-22)
- SQL-миграция `supabase/migrations/0001_videos.sql` — таблица `videos` со всеми полями из блоков 1–4, индексы, триггер `updated_at`, RLS включён с публичными политиками (добавим auth позже)
- Установлен `@supabase/supabase-js`, создан клиент `frontend/src/lib/supabaseClient.ts` (браузерный + серверный)
- Сервис `frontend/src/lib/videosService.ts` — `fetchVideos()` с маппингом snake_case → camelCase
- Описание столбцов `frontend/src/lib/videoTableColumns.ts` — 15 столбцов, сгруппированы по блокам (Справка / Статистика / AI / Мои пометки)
- Компоненты таблицы `frontend/src/components/blog/analyst/`: `VideoTable.tsx`, `VideoTableHeader.tsx` (две строки: группы + столбцы), `VideoTableRow.tsx` (с форматированием дат и чисел на русском)
- Страница `/blog/analyst` рендерится динамически (`export const dynamic = "force-dynamic"`), тянет данные из Supabase на каждый запрос

**Сессия 3: Добавление одного видео + адаптивность таблицы** (завершена 2026-04-22)
- Server Action `frontend/src/lib/actions/addVideo.ts` — валидация Instagram Reels ссылки по regex (`/reel/` или `/reels/`), insert `url` в `videos`, `revalidatePath("/blog/analyst")`, обработка уникальной коллизии (код 23505 → «Это видео уже в таблице»)
- `AddVideoModal.tsx` (клиент) — форма с `useActionState` + `useFormStatus`, autofocus, блокировка скролла фона, закрытие по Esc и клику на подложку, автосброс после успеха
- `AddVideoButton.tsx` — тонкая обёртка, заменила disabled-кнопку на странице
- Пустое состояние вынесено из `<table>` в `VideoTableEmpty.tsx` — центрированный текст больше не обрезается широкой горизонтальной прокруткой таблицы
- `VideoTableSection.tsx` (клиент) — тулбар со счётчиком и кнопкой fullscreen (`Maximize2`/`Minimize2`). В fullscreen — `fixed inset-0 z-[60]` поверх бокового меню, блокирует скролл страницы, закрывается по Esc
- Адаптивность шелла: `<main>` получил `overflow-x-hidden`, внутренний контейнер — `min-w-0`, отступы на мобильных уменьшены (`px-4`)
- `VideoTable` — flex-колонка с `overflow-auto`, корректно скроллится по обеим осям в fullscreen и ограничивает горизонтальный скролл своим контейнером вне fullscreen

**Сессия 4: Парсинг данных через Apify** (завершена 2026-04-26, протестирована локально)
- Миграция `supabase/migrations/0002_processing_status.sql` — добавлены `processing_status` (pending/processing/done/error), `processing_error`, `processed_at` + индекс
- Бэкенд `backend/` на Express (ESM, Node ≥ 20):
  - `src/config/env.js` — валидация APIFY_API_TOKEN, SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY (именно service role)
  - `src/services/apifyService.js` — вызов `apify/instagram-scraper` через `apify-client`, маппинг в snake_case, расчёт вирусности `(лайки + 2×коммент)/просмотры × 100`
  - `src/services/supabaseService.js` — service-role клиент, функции `markVideoProcessing` / `saveVideoSuccess` / `saveVideoError`
  - `src/services/videoProcessor.js` — оркестратор: id → Apify → Supabase. Ошибки пишет в `processing_error`, не бросает наружу
  - `src/routes/videos.js` — `POST /api/videos/process` с `{ videoId }`, валидирует uuid, отвечает 202, обрабатывает в фоне
  - `src/app.js` — CORS (whitelist из `FRONTEND_ORIGINS`), JSON body, `GET /health`
- Фронтенд:
  - `src/lib/types.ts` — тип `ProcessingStatus` и поля `processingStatus` / `processingError` / `processedAt` в `Video`. Поле `shares` убрано из `Video` (Instagram не отдаёт)
  - `src/lib/videosService.ts` — читает новые колонки, дефолт `pending`
  - `src/lib/backendClient.ts` — `triggerVideoProcessing(videoId)` с таймаутом 5 сек, глотает ошибки сети
  - `src/lib/actions/addVideo.ts` — после insert получает id и дёргает бэкенд
  - `src/components/blog/analyst/VideoTableRow.tsx` — показывает «В очереди», «Обрабатывается…» (с пульсирующей точкой) или «Ошибка обработки» в пустых ячейках; текст ошибки в `title` tr
  - `src/lib/videoTableColumns.ts` — столбец «Шеры» удалён
  - `src/app/api/thumbnail/route.ts` — серверный прокси для превью с Instagram CDN (whitelist хостов `cdninstagram.com` / `fbcdn.net` / `instagram.com`, `cache-control: max-age=86400`). Картинка грузится через `/api/thumbnail?url=...`, иначе Instagram отдаёт 403 из-за Referer
  - `AddVideoModal.tsx` — подсказка про автопарсинг

Что выяснилось при запуске (записано в «Известные ограничения»):
- `localhost` на Windows + Node 18+ резолвится в IPv6 `::1`, бэкенд слушает IPv4 → тайм-аут. В `frontend/.env.local` нужен **`BACKEND_URL=http://127.0.0.1:3001`**, не `localhost`.
- `node --watch` (`npm run dev`) перезапускает бэкенд при касании файлов и убивает обработку Apify на полпути → строка зависает в `processing`. Локально запускать бэкенд через **`npm start`** (без watch).
- Instagram CDN отдаёт превью с временной подписью (~24ч) и с защитой от Referer — сделан прокси `/api/thumbnail`. Долгосрочное решение — скачивать превью при парсинге в Supabase Storage (отдельная сессия).
- «Шеры» Instagram больше не отдаёт публично — столбец удалён из UI. Колонка `shares` в БД оставлена на случай возвращения через другой источник.

Ручные шаги для Влада (если нужно поднять заново):
1. **Миграция Supabase**: Dashboard → SQL Editor → вставить `supabase/migrations/0002_processing_status.sql` → Run.
2. **Установить зависимости бэкенда** (один раз): PowerShell в `backend/` → `& "C:\Program Files\nodejs\npm.cmd" install`.
3. **Добавить в `frontend/.env.local`**: `BACKEND_URL=http://127.0.0.1:3001`.
4. **Запустить бэкенд** в отдельном терминале из `backend/`: `& "C:\Program Files\nodejs\npm.cmd" start`.
5. **Запустить фронт** в другом терминале из `frontend/`: `& "C:\Program Files\nodejs\npm.cmd" run dev`.
6. http://localhost:3000/blog/analyst → «Добавить видео» → вставить Reels. Через 30–60 сек после обновления страницы — автор, обложка, статистика.
7. **Деплой на Railway** — отдельной сессией: создать проект, залить `backend/`, прокинуть переменные из `.env`, после деплоя в `frontend/.env.local` и в переменных Vercel заменить `BACKEND_URL` на URL Railway.

**Сессия 5: Транскрипция через Whisper** (завершена 2026-04-26)
- Решено использовать **OpenAI Whisper** вместо AssemblyAI — тот же ключ `OPENAI_API_KEY` пригодится в Сессии 6 для саммари/категоризации, не плодим интеграции
- Миграция `supabase/migrations/0003_transcript_status.sql` — отдельный статус транскрипции `transcript_status` (`pending` / `processing` / `done` / `no_speech` / `error`) + `transcript_error`. Не сливаем с `processing_status`, чтобы падение Whisper не превращало всю строку в «ошибка обработки»
- Установлены `openai` (^6) и `ffmpeg-static` (^5) в `backend/`. В `backend/.env.example` добавлены `OPENAI_API_KEY` и опциональный `OPENAI_WHISPER_MODEL` (по умолчанию `whisper-1`)
- `backend/src/config/env.js` — валидирует `OPENAI_API_KEY` как required
- `backend/src/services/apifyService.js` — добавлен `extractVideoUrl(raw)`, читает `videoUrl` (с фолбэком `videoUrlBackup`). В таблицу не пишем — ссылка временная, нужна только в рамках текущей обработки
- `backend/src/services/transcriptionService.js` — основной модуль:
  - **Извлекаем аудио, не отправляем сам mp4.** Whisper ограничен 25 МБ на файл, видео Reels уже на 30 сек могут весить 30–40 МБ. ffmpeg вытаскивает только звуковую дорожку: mp3, 64 kbps, моно, 16 kHz — речь читается отлично, размер падает в 20–50 раз. ffmpeg вызывается через `child_process.spawn` с бинарём из `ffmpeg-static` (нет системной зависимости, на Railway тоже работает). Все временные файлы — в `os.tmpdir()`, чистятся в `finally`
  - `downloadVideoToFile(url, path)` — потоково пишет mp4 на диск, без Referer (Instagram CDN отдаёт 403 на «чужой» Referer)
  - Whisper вызывается через `openai.audio.transcriptions.create({ file, model, language: "ru", response_format: "text" })` — `language: ru` это hint, не constraint, английская речь тоже распознаётся
  - Эвристика «нет речи»: после очистки от `[музыка]`, `♪`, пробелов остаётся <4 символов → `{ status: "no_speech", text: null }`. У Whisper нет явного флага «no speech», иначе пришлось бы детектить по постпроцессу
  - Возвращает `{ status: "done", text }` либо `{ status: "no_speech", text: null }`. Любая ошибка бросается наружу, ловит её процессор
- `backend/src/services/supabaseService.js` — добавлены `markTranscriptProcessing` / `saveTranscriptSuccess` / `saveTranscriptNoSpeech` / `saveTranscriptError`
- `backend/src/services/videoProcessor.js` — теперь два шага: сначала Apify (как раньше), потом транскрипция. Apify-ошибка → весь `processing_status = error` и стоп; ошибка транскрипции — изолированно в `transcript_status = error`, остальные данные остаются. Если Apify не вернул `videoUrl`, транскрипция помечается ошибкой с понятным текстом
- Фронт:
  - `frontend/src/lib/types.ts` — `TranscriptStatus` и поля `transcriptStatus` / `transcriptError` в `Video`
  - `frontend/src/lib/videosService.ts` — читает новые колонки, дефолт `pending`
  - `frontend/src/components/blog/analyst/VideoTableRow.tsx` — отдельная функция `renderTranscriptCell`: «Расшифровывается…» (с пульсацией) / «Без речи (музыка/визуал)» / «Ошибка расшифровки» (текст ошибки в `title`) / «Ждёт расшифровки» / сам текст в две строки

Ручные шаги для Влада (после pull):
1. **Миграция Supabase**: Dashboard → SQL Editor → вставить `supabase/migrations/0003_transcript_status.sql` → Run.
2. **Зависимости бэкенда**: PowerShell в `backend/` → `& "C:\Program Files\nodejs\npm.cmd" install` (поставит `openai`).
3. **Проверить `backend/.env`**: `OPENAI_API_KEY=...` уже добавлен — никаких лишних переменных не нужно.
4. Запустить бэкенд (`npm start` в `backend/`) и фронт (`npm run dev` в `frontend/`), как раньше.
5. Добавить новое видео — через 1–2 минуты в столбце «Транскрипция» появится текст или «Без речи».

**Сессия 6: AI-анализ + UX модалка для длинного текста** (завершена 2026-04-26)
- Миграция `supabase/migrations/0004_ai_status.sql` — статус `ai_status` (`pending` / `processing` / `done` / `skipped` / `error`) + `ai_error`. Поля `ai_summary` / `ai_category` / `ai_category_suggestion` уже были из миграции 0001 — здесь только статус и ошибка
- `backend/src/services/aiAnalysisService.js` — анализ через `gpt-4o-mini` (модель в env: `OPENAI_ANALYSIS_MODEL`, дефолт `gpt-4o-mini`):
  - Системный промпт на русском, объясняет роль («помощник блогера, разбирающего Reels»), формат саммари (2–3 предложения, без вводных, уверенно), список ключей категорий с русскими подписями. Если ничего не подходит — `category: "other"` + `category_suggestion` (1–3 слова на русском)
  - JSON-режим (`response_format: { type: "json_object" }`), `temperature: 0.3` — стабильность важнее креатива
  - Если модель вернула невалидный ключ категории — мягко скатываем в `other`, сохраняя её ответ как `category_suggestion`. Промахи бывают — лучше показать Владу, чем падать
  - Caption обрезается до 2000 символов, транскрипция — до 8000 (на случай длинных видео, чтобы не отправлять в OpenAI «Войну и мир»)
  - Если нет ни caption, ни транскрипции — возвращает `{ status: "skipped" }`, в БД `ai_status = "skipped"`, в UI «Недостаточно данных»
- `backend/src/services/supabaseService.js` — добавлены `markAiProcessing` / `saveAiSuccess` / `saveAiSkipped` / `saveAiError`
- `backend/src/services/videoProcessor.js` — теперь три шага: Apify → Whisper → AI. Каждый шаг изолирован своим статусом, падение позднего шага не обнуляет данные ранних. **AI запускается даже если Whisper упал** — caption из Apify обычно есть, и саммари по нему всё равно полезное
- Фронт:
  - `frontend/src/lib/types.ts` — `AiStatus` + поля `aiStatus` / `aiError` в `Video`
  - `frontend/src/lib/videosService.ts` — читает новые колонки, дефолт `pending`
  - `frontend/src/lib/aiCategories.ts` — `aiCategoryLabels` (slug → русское название) и `formatAiCategory(category, suggestion)`. Для `other` с подсказкой выводим как «Другое: <подсказка>». **Ключи синхронизированы с CATEGORIES в backend** — менять одновременно
  - `frontend/src/components/blog/analyst/VideoTableRow.tsx` — `renderSummaryCell` (статусы анализа + текст в модалке) и `renderCategoryCell` (pill-стиль с границей и фоном)
- **UX-фикс длинного текста**: новый компонент `frontend/src/components/blog/analyst/ExpandableText.tsx` (клиентский). Превью с `line-clamp`, по клику — модалка с полным текстом (заголовок, кнопка «закрыть», Esc, клик по подложке). Использован для столбцов «Транскрипция», «Саммари», «Описание». Превью оформлено как кнопка, чтобы курсор показывал «кликабельно», в `title` — подсказка «Нажмите, чтобы прочитать целиком»

Ручные шаги для Влада (после pull):
1. **Миграция Supabase**: Dashboard → SQL Editor → вставить `supabase/migrations/0004_ai_status.sql` → Run.
2. Перезапустить бэкенд (`npm start` в `backend/`) и фронт (`npm run dev` в `frontend/`).
3. Добавить новое видео. Хронология появления данных:
   - 30–60 сек → Apify-поля (автор, статистика, превью)
   - +30–60 сек → транскрипция (или «Без речи»)
   - +5–15 сек → саммари и категория AI
4. Кликнуть по саммари / транскрипции / описанию — откроется модалка с полным текстом.

**Сессия 7: Ручные поля и фильтрация — Notion-стиль** (завершена 2026-04-26)
- Миграция `supabase/migrations/0005_manual_fields.sql`:
  - Новые таблицы `my_categories` и `tags` (id, name unique, color, created_at) — справочники категорий/тегов пользователя
  - Join-таблица `video_tags(video_id, tag_id)` с PK по обоим колонкам и каскадным удалением
  - `videos.my_category` (text) **удалён**, добавлен `my_category_id uuid` с `on delete set null` → FK на `my_categories`. Реальных данных в этом поле не было (UI ввода до сессии 7 не существовало)
  - `videos.tags` (text[]) **удалён** — теперь связь через `video_tags`
  - Стартовый набор тегов засеивается: 🪝 Хук (amber), 🎨 Визуал (purple), 🎯 Тема (blue)
  - RLS открытая (как у `videos`) — приложение пока без auth
- Палитра цветов `frontend/src/lib/tagColors.ts` — 11 цветов (gray/red/orange/amber/yellow/green/teal/blue/indigo/purple/pink), для каждого литеральные классы Tailwind (chip / swatch / hover). `pickNextColor(usedCount)` раздаёт цвета по кругу из «нескучных» — соседние теги визуально различаются. **Литеральные классы — обязательно**: динамические `bg-${color}-100` JIT-Tailwind не видит и не кладёт в бандл
- Типы: `MyCategory`, `Tag` (id/name/color), `Video.myCategoryId`, `Video.tagIds: string[]` (id-шники привязанных тегов)
- Сервисы:
  - `videosService.ts` — `fetchVideos()` теперь джойнит `video_tags(tag_id)` через embedded select Supabase, добавлены `fetchMyCategories()` и `fetchTags()`
  - `manualFieldsService.ts` — браузерные мутации (RLS открыта на anon): create/rename/recolor/delete для категорий и тегов, attach/detach для связи, setVideoMyCategory, setVideoNote. **Идём напрямую в Supabase из браузера, не через server actions** — даёт мгновенный отклик без revalidatePath
  - `videoFilters.ts` — чистые `applyFilters()` и `uniqueAuthors()`. Фильтрация по AI-категории, моей категории, тегам (intersection — Notion-стиль), автору, наличию транскрипции; сортировка по createdAt/publishedAt/views/likes/virality, поиск по транскрипции/саммари/описанию/заметкам
- Компоненты `frontend/src/components/blog/analyst/`:
  - `EntityChip.tsx` — цветная пилюля (sm/md), используется и в ячейках, и в фильтрах
  - `EntitySelectPopover.tsx` — Notion-style поповер выбора. Поиск + список с галочкой выбранного + «Создать "xxx"» если точного совпадения нет + «Управление» снизу. Позиционирование `position: fixed` через `getBoundingClientRect()` якоря + `createPortal` в `document.body` (чтобы не клипалось `overflow:hidden` таблицы), закрывается по Esc/клику вне/скроллу страницы. `onCreate` опциональный — если не передан, кнопка создания не показывается (для фильтра тегов)
  - `MyCategoryCell.tsx` — single-select (повторный клик снимает категорию)
  - `TagsCell.tsx` — multi-select (поповер не закрывается, можно накидать сразу несколько)
  - `NoteCell.tsx` — превью + поповер с textarea, **автосохранение на Esc и при клике вне** (как в Notion: ушёл — сохранилось). Если значение не изменилось — запрос не отправляется
  - `EntityManageModal.tsx` — управление справочником: инлайн-переименование (Enter/blur), смена цвета через свотчер с мини-палитрой, удаление с `confirm()`. Используется и для категорий, и для тегов (одна модалка с разными пропсами)
  - `FilterBar.tsx` — поиск + чипы фильтров (категория AI, категория Я, теги через тот же EntitySelectPopover, автор, транскрипция) + сортировка (select по полю + кнопка направления). При выбранных тегах ниже показывается строка с чипами и крестиком на каждом. Кнопка «Сбросить» появляется когда есть отличия от дефолта
  - `AnalystWorkspace.tsx` — клиентский корень. Держит state видео/категорий/тегов/фильтров/fullscreen/режима management-модалки. Все мутации делает оптимистично: сначала setState, потом запрос в Supabase, alert при ошибке. **Синкает state с props через useEffect** — после `revalidatePath` от `addVideoAction` страница даёт новые initialVideos, и состояние обновляется
  - `VideoTableRow.tsx` — теперь клиентский, принимает `myCategories`, `tags` и набор коллбэков через интерфейс `VideoTableRowCallbacks`
  - `VideoTable.tsx` — клиентский, прокидывает коллбэки строкам
  - `VideoTableSection.tsx` — **удалён**, его роль (тулбар + fullscreen) переехала в `AnalystWorkspace`
- Страница `/blog/analyst` тянет `videos`, `my_categories`, `tags` параллельно через `Promise.all` и передаёт в `AnalystWorkspace`

Ручные шаги для Влада (после pull):
1. **Миграция Supabase**: Dashboard → SQL Editor → вставить `supabase/migrations/0005_manual_fields.sql` → Run. ⚠️ Миграция дропает столбцы `videos.my_category` и `videos.tags` — реальных данных там не было (UI ввода до сессии 7 не существовало), но если ты вручную что-то писал в БД — сделай бэкап
2. Перезапустить фронт (`npm run dev` в `frontend/`). Бэкенд не трогали — перезапускать не надо
3. Открыть видео в таблице. В колонках «Категория Я» и «Теги» — клик → поповер. Введи имя → Enter → создалось. По клику на «Управление» снизу — модалка с переименованием/сменой цвета/удалением
4. В колонке «Заметка» — клик → ввёл → Esc или клик вне сохраняет
5. Над таблицей — панель фильтров. Поиск по тексту, фильтры по AI-категории/категории Я/тегам/автору/транскрипции, сортировка с переключателем направления

**Сессия 8: Удаление и массовые действия** (завершена 2026-04-26)
- `frontend/src/lib/videoDeleteService.ts` — браузерные `deleteVideo(id)` и `deleteVideos(ids)`. Идём напрямую в Supabase (RLS открыта), массовое удаление одним запросом через `.in("id", ids)`. Связи `video_tags` подчищаются каскадом из миграции 0005 — отдельная очистка не нужна
- `frontend/src/components/blog/analyst/ConfirmDialog.tsx` — переиспользуемый диалог подтверждения с заголовком, описанием, кнопками «Отмена» / «Удалить». Поддерживает `tone="danger"` (иконка-предупреждение, акцентный цвет на кнопке), Esc/Enter, блокировку фона, состояние `busy`. Заменяет нативный `confirm()` для удаления видео — нужен дизайн и человеческая фраза «Удалить N видео?»
- `frontend/src/components/blog/analyst/BulkActionBar.tsx` — плавающая `fixed`-панель снизу по центру (`z-[80]`), появляется когда выбрана ≥1 строка. Текст «Выбрано: N видео» + кнопки «Удалить» (акцентная) и «×» (снять выделение)
- `VideoTableHeader` — добавлена служебная колонка слева с чекбоксом «выбрать все». Состояние `none` / `all` / `some`: для `some` через ref ставим нативный `indeterminate` (React не поддерживает его как prop). `rowSpan={2}`, чтобы чекбокс висел вертикально по центру обеих строк шапки
- `VideoTableRow` — служебная ячейка с чекбоксом и кнопкой-корзинкой (`Trash2`), которая видна только при hover строки (или фокусе кнопки) — чтобы не загромождать UI. Выбранная строка подсвечена `bg-accent-soft/40`. Удаление одной строки идёт через тот же `ConfirmDialog`, что и массовое
- `VideoTable` — принимает `selectedIds: Set<string>` и `onToggleAll`. `deriveSelectionState(filtered, selectedIds)` считает галочку шапки относительно **отфильтрованных** видео (а не всех): «выбрать все» работает для текущего среза — прямая аналогия с фильтром «Мусор» из CLAUDE.md
- `AnalystWorkspace`:
  - State: `selectedIds: Set<string>` + `pendingDelete: { kind: "single" | "bulk", … } | null` + `deleteBusy`
  - **Чистка stale-id**: useEffect синкает `selectedIds` со списком видео — после удаления / нового `initialVideos` от revalidatePath исключаем id, которых уже нет. Без этого после refresh «выбрано: N видео» висит на пустом месте
  - `handleToggleAll` — переключает выделение на всех **отфильтрованных** видео, не на всём списке
  - `confirmDelete` — оптимистичное удаление: сразу режем state, при ошибке откатываем по snapshot. Один путь и для single, и для bulk — отличается только формой `pendingDelete`
- БД-миграции **не нужны** — каскад `on delete cascade` на `video_tags` уже был с сессии 7

**Сессия 10: Полировка** (завершена 2026-04-26)
- Поддержка ссылок формата `/p/` — расширен regex на `(reel|reels|p)` в `addVideo.ts` и `reelsUrlParser.ts`
- Мобильная адаптация (таблица → карточки)
- Кнопка «повторить обработку» для строк со статусом `error` или застрявших `processing`
- Обработка ошибок (невалидная ссылка, Apify не ответил)
- Пустые состояния

Ручные шаги для Влада (после pull):
1. Перезапускать ничего не нужно — это чисто фронтовая сессия. Если фронт уже запущен, изменения подтянутся hot reload-ом, иначе `npm run dev` в `frontend/`
2. В таблице слева у каждой строки появилась галочка. При наведении на строку справа от галочки виден значок корзины — клик на него удаляет одно видео (с подтверждением)
3. Поставь галочки на нескольких видео → внизу появится плавающая панель «Выбрано: N видео» → «Удалить» → подтверждение → удалит всё разом
4. Галочка в шапке выделяет всё, что сейчас в таблице (с учётом активных фильтров) — удобно для сценария «отфильтровал «Мусор» → выделил всё → удалил»

**Сессия 9: Массовое добавление** (завершена 2026-04-26)
- Миграция `supabase/migrations/0006_processing_status_index.sql` — частичный индекс на `processing_status` (`where processing_status in ('pending', 'processing')`) для быстрого recovery-сканирования. Уникальный индекс на `url` уже был с миграции 0001
- Бэкенд:
  - `backend/src/queue/workerPool.js` — пул из N воркеров с in-memory очередью и дедупом через `Set`. `enqueue(id)` / `enqueueMany(ids)` / `getQueueStats()`. Конкурентность настраивается через env `WORKER_CONCURRENCY` (дефолт 2). Дедуп нужен, чтобы повторный enqueue одного и того же id не пускал его в работу второй раз
  - `backend/src/services/recoveryService.js` — при старте сканирует БД на `pending` + `processing` строки и заливает их в очередь. Без этого после перезапуска Railway пользователь сидит и ждёт зря
  - `backend/src/services/videoProcessor.js` — больше **не пропускает** строки со статусом `processing`, считая их «застрявшими» после рестарта. От повторной обработки в рамках одного процесса защищает in-memory Set воркер-пула
  - `backend/src/services/supabaseService.js` — добавлена `getUnfinishedVideoIds()` для recovery
  - `backend/src/routes/videos.js` — `POST /api/videos/process` теперь идёт через `enqueue` (вместо прямого fire-and-forget). Добавлен `POST /api/videos/process-batch` принимающий `{ videoIds: string[] }` (макс. 5000 за запрос). Добавлен `GET /api/videos/queue/status` для отладки
  - `backend/src/index.js` — конфигурация пула + вызов recovery после `app.listen`
  - `backend/.env.example` — `WORKER_CONCURRENCY=2` с пояснением (3-4 ускорит 1000-видео-пачку с ~8ч до ~4ч, но рискованно по rate-limit Apify)
- Фронтенд:
  - `frontend/src/lib/reelsUrlParser.ts` — `parseReelsList(text)` режет ввод по `\n,;`, нормализует URL (отрезает query, форсит `www.instagram.com`), отбрасывает дубли в рамках пачки и сохраняет образец невалидных строк (до 10) для UI. Лимиты: `MAX_BATCH_SIZE = 2000`, `WARN_BATCH_SIZE = 500`. `estimateBatch(count)` возвращает грубую оценку времени и стоимости (Apify $0.003-0.008 на видео + Whisper)
  - `frontend/src/lib/actions/addVideoBatch.ts` — server action: парсит, валидирует лимит, делает `upsert(..., { onConflict: "url", ignoreDuplicates: true })` пачками по 250 строк. **Без `ignoreDuplicates` любая существующая ссылка валит весь чанк** unique violation'ом. После insert одним POST шлёт все id на бэкенд через `triggerVideoProcessingBatch`. Возвращает детализированный state: parsed/inserted/duplicates/invalid
  - `frontend/src/lib/backendClient.ts` — `triggerVideoProcessingBatch(ids)` (таймаут 15 сек — длиннее single, на случай холодного старта Railway)
  - `frontend/src/components/blog/analyst/AddVideoBatchModal.tsx` — модалка с textarea (controlled), live-предпросмотром «распознано / дублей / невалидных» при каждом изменении, плашкой про время+стоимость для пачек ≥500, hard-stop про лимит 2000. Сабмит блокируется если 0 валидных или превышен лимит
  - `frontend/src/components/blog/analyst/AddVideoButton.tsx` — две кнопки: «Добавить видео» (primary, single) + «Добавить много» (secondary, batch)
  - `frontend/src/components/blog/analyst/BatchProgressBar.tsx` — рисуется только если `inFlight > 0`. Знаменатель прогресса — все видео созданные за последние 24ч (close enough к «текущей пачке», без новой таблицы batches). Каждые 5 сек дёргает `router.refresh()` пока есть незавершённые; `setInterval` сам останавливается через cleanup, как только всё done/error
  - `AnalystWorkspace.tsx` — прогресс-бар вставлен над FilterBar

Решения, принятые намеренно:
- **Без таблицы batches.** Глобальный счётчик «pending+processing за 24ч» покрывает реальный сценарий Влада (раз в день вставить пачку). Если позже захочется видеть «вот эти 50 я только что добавил» отдельно от старой пачки — добавим тогда
- **Дедуп через `upsert ignoreDuplicates`.** Один round-trip вместо select-then-insert, unique-конфликт ловит БД
- **Recovery подхватывает и `processing`, и `pending`.** «Processing»-строка после рестарта — это по факту брошенная задача, без recovery она бы висела вечно

Известные ограничения сессии 9:
- Если бэкенд перезапустится посреди обработки — текущая Apify/Whisper-задача теряется, статус строки откатится в `processing`, recovery при следующем старте подхватит и **запустит её с нуля** (внутреннего чекпойнта между шагами нет — перебор для текущего масштаба)
- Прогресс-бар не различает «эту пачку» и старые висящие в processing строки. Лечится удалением такой строки или повторной обработкой (кнопка retry — задача на сессию 10)

Ручные шаги для Влада (после pull):
1. **Миграция Supabase**: Dashboard → SQL Editor → вставить `supabase/migrations/0006_processing_status_index.sql` → Run.
2. **Перезапустить бэкенд** (`Ctrl+C` в окне бэкенда, потом `& "C:\Program Files\nodejs\npm.cmd" start` в `backend/`). При старте в логах увидишь `Worker pool : concurrency=2` и `[recovery] ...`.
3. **Перезапустить фронт** (`Ctrl+C` и `& "C:\Program Files\nodejs\npm.cmd" run dev` в `frontend/`).
4. На странице Аналитика появилась вторая кнопка **«Добавить много»**. Нажми → модалка с textarea. Вставь список ссылок (по одной на строку, можно запятыми/`;`), сразу под textarea — live-предпросмотр: «Распознано: N, Дублей: K, Невалидных: M».
5. Если в пачке ≥500 ссылок — появится жёлтое предупреждение с оценкой времени и стоимости (~$3–7 на 1000 видео). Если >2000 — кнопка заблокируется.
6. Жми «Запустить обработку» — модалка закроется, над таблицей появится прогресс-бар «Обработка видео: 0 из N», начнёт двигаться. Браузер можно закрыть.
7. Опциональная переменная `WORKER_CONCURRENCY` в `backend/.env` (по умолчанию 2). Если уверен, что Apify-план тянет — поставь 3 или 4, обработка ускорится в 1.5–2 раза.

**Сессия 11: UI-правки + переименование + auto-hide меню** (завершена 2026-04-26)
- **Переименование «Аналитик» → «База референсов»**:
  - `nav.ts` — пункт переименован, иконка `BookmarkCheck`
  - URL: новый маршрут `frontend/src/app/blog/references/page.tsx` (копия страницы с заголовком «База референсов» и хлебными крошками «Блог · Инструменты»). Старый `/blog/analyst` теперь редиректит на новый через `redirect()`
  - `revalidatePath("/blog/analyst")` → `/blog/references` в `actions/addVideo.ts` и `actions/addVideoBatch.ts`. Главная (`app/page.tsx`) тоже редиректит на `/blog/references`
  - Папку `frontend/src/components/blog/analyst/` оставил под старым именем — это внутренняя организация, не URL и не UI. Переименование требует обновления десятков импортов без пользовательской выгоды
- **Иерархия меню Блог → Инструменты / Команда**:
  - `nav.ts` теперь типизирован тремя уровнями: `NavSection { groups: NavGroup[] }`, `NavGroup { items: NavLeaf[] }`, `NavLeaf { href? }`. Если `href` нет — это плейсхолдер (для «Скоро» в Команде)
  - Иконки: `Wrench` для «Инструменты», `Users` для «Команда»
  - Новый компонент `SidebarSection.tsx` — рендерит секцию с её группами. `SidebarGroup.tsx` переписан: теперь представляет группу (Инструменты/Команда), а не секцию верхнего уровня. `SidebarItem.tsx` обработал `comingSoon`: для пунктов без href рисует не-кликабельный блок с серой плашкой «скоро»
- **Auto-hide бокового меню на десктопе**:
  - `AppShell.tsx` держит state `desktopOpen`. Меню по умолчанию скрыто (`lg:-translate-x-full`), рабочая область занимает всю ширину (убрал `lg:pl-72`). Анимация `transition-transform duration-200`
  - Hover-зона: невидимый `<div>` 20px (`w-5`) у левого края с `onMouseEnter={() => setDesktopOpen(true)}`. `Sidebar` через `onMouseLeave` сбрасывает обратно в false
  - Индикатор-полоска: `w-[3px] bg-accent/30` у левого края, `pointer-events-none`, всегда видна на десктопе. Когда меню выезжает — закрывается им сверху (z-index у меню выше)
  - На мобилке поведение прежнее (бургер). Условные классы `lg:translate-x-0`/`lg:-translate-x-full` переключаются по `desktopOpen`, мобильный класс — по `mobileOpen`
- **Дата публикации = гиперссылка**:
  - В `VideoTableRow.tsx`, кейс `publishedAt`: текст даты обёрнут в `<a href={video.url} target="_blank" rel="noreferrer">`. Стилизация — `text-ink` (без синего) + `underline-offset-4 hover:underline`. `title="Открыть видео в Instagram"`
- **Превью с увеличением**:
  - `ImagePreviewModal.tsx` — модалка с тёмной полупрозрачной подложкой, картинкой `max-h-[90vh] max-w-[90vw] object-contain` и кнопкой ×. Esc/клик по подложке закрывают, body-скролл блокируется
  - `ThumbnailCell.tsx` — отдельный клиентский компонент с локальным state `open`. Кнопка-превью с `cursor-zoom-in`, при клике открывает модалку. Прокси `/api/thumbnail` сохранил, чтобы Instagram CDN отдавал картинки
  - В `VideoTableRow.tsx` кейс `thumbnail` упростился до `<ThumbnailCell url={video.thumbnailUrl} />`

Ручные шаги для Влада (после pull):
1. Перезапускать ничего не нужно — это чисто фронтовая сессия. Если фронт уже запущен, изменения подтянутся hot reload-ом, иначе `& "C:\Program Files\nodejs\npm.cmd" run dev` в `frontend/`
2. Старая ссылка `http://localhost:3000/blog/analyst` редиректит на новую `http://localhost:3000/blog/references` — закладку можно не трогать, обновится сама. Главная страница тоже теперь ведёт на новую
3. Двигай мышь к самому левому краю экрана — там тонкая оранжевая полоска. Подведи мышь — выедет меню. Уведи мышь — спрячется
4. В меню теперь иерархия: «Блог» → внутри «Инструменты» (там «База референсов») и «Команда» (там «Скоро» — серая плашка, пока неактивно)
5. В таблице кликай по дате публикации — откроется видео в Instagram в новой вкладке. Кликай по превью — откроется модалка с увеличенной картинкой (Esc закрывает)

---

**Сессия 12: Формула вирусности + исследование шеров** (завершена 2026-04-26)
- **Исследование шеров**:
  - `apify/instagram-scraper` (наш текущий актор, дефолт `APIFY_ACTOR_ID`) — `sharesCount` **не возвращает**. Подтверждено документацией актора + issue-трекером (тикеты «Scraping reels Shares» висят, не починены)
  - `apify/instagram-reel-scraper` — **возвращает `sharesCount`**. Это специализированный актор только под Reels, поля совместимы с нашими (`ownerUsername`, `caption`, `displayUrl`, `videoUrl`, `videoPlayCount`, `videoViewCount`, `likesCount`, `commentsCount`, `timestamp`) + дополнительно `sharesCount`
  - **Решение**: код умеет читать `sharesCount` из ответа любого актора. Дефолтный актор оставлен как был (`apify/instagram-scraper`) — переключение на `apify/instagram-reel-scraper` потребует тестирования, делать это не в рамках этой сессии. Если когда-нибудь понадобится — поменять `APIFY_ACTOR_ID=apify/instagram-reel-scraper` в `backend/.env`, никаких изменений в коде не нужно. В дефолтной конфигурации шеры остаются `null`, формула работает без них
- **Новая формула вирусности** в `backend/src/services/apifyService.js` → `computeVirality({ views, likes, comments, shares })`:
  - `((likes × 1) + (comments × 3) + (shares × 5)) / views × 100`
  - Веса: коммент ценнее лайка (порог входа выше — нужно реально что-то напечатать), шер ценнее коммента (распространяет в чужой контент). Если `shares = null` — слагаемое равно 0, формула продолжает работать
  - Старая формула `(likes + 2×comments) / views × 100` заменена. Существующие строки в БД пересчитаются только при повторной обработке (через retry или на новых видео); пересчитывать вручную через миграцию не стал — для текущего объёма данных у Влада не критично, и формулу мы ещё будем калибровать
- **Поле `shares`** — теперь пишется в БД при парсинге (если актор отдал). В UI столбец «Шеры» по-прежнему скрыт — отображать его нет смысла, пока работаем на дефолтном акторе и поле всегда null. Если переключим актор — добавим колонку отдельной правкой
- **Новый столбец «Уровень»** в группе «Статистика», справа от «Вирусности»:
  - Конфиг порогов в `frontend/src/lib/viralityLevel.ts` → `VIRALITY_LEVELS`. Меняешь пороги или подписи там — обновляется и логика, и отображение. Пороги сейчас:
    - score ≥ 10 → «Вирусное 🔥» (красная пилюля)
    - 5 ≤ score < 10 → «Выше среднего» (янтарная)
    - 2 ≤ score < 5 → «Среднее» (синяя)
    - score < 2 → «Низкое» (серая)
  - Рендер — pill в стиле существующих категорий, использует палитру `ENTITY_COLORS` из `tagColors.ts`. Шапка таблицы и группировка добавляются автоматически (`videoTableColumns.ts` — единственный источник правды для столбцов)
  - В `VideoTableRow.tsx` — отдельный case `viralityLevel`: если score есть → pill, иначе по статусу обработки (placeholder / «обрабатывается…»)

Решения, принятые намеренно:
- **Не пересчитываем вирусность для уже обработанных видео.** Формула — чувствительная вещь, мы её ещё будем калибровать на реальных данных Влада. Пересчёт миграцией зафиксирует текущие коэффициенты в истории; лучше чтобы новые значения появлялись по мере обработки новых видео и retry старых
- **Не добавляем фильтр по уровню в FilterBar.** Сортировка по `virality` уже есть и она удобнее: показывает упорядоченный спектр, а не группы. Если позже окажется, что фильтр «только вирусные» нужен — добавим (≤30 минут работы)
- **Шеры в UI не показываем.** Пока актор не переключён — поле всегда null, столбец будет пустой. Чтобы избежать визуального шума, оставляем его скрытым

Ручные шаги для Влада (после pull):
1. Это чисто кодовая правка — миграции БД нет, env-переменные не меняются. Перезапустить **бэкенд** (`Ctrl+C` в окне бэкенда → `& "C:\Program Files\nodejs\npm.cmd" start` в `backend/`) — без рестарта новые видео всё ещё будут считаться по старой формуле. **Фронт** перезапускать не обязательно (hot reload подхватит новый столбец), но если он не запущен — `& "C:\Program Files\nodejs\npm.cmd" run dev` в `frontend/`
2. Открой `http://localhost:3000/blog/references` — справа от столбца «Вирусность» появится новый столбец «Уровень» с цветными плашками
3. У старых видео уровень посчитан по старой формуле вирусности — новые значения появятся, когда добавишь новые видео или нажмёшь «повторить обработку» на существующих
4. Если хочешь, чтобы Apify начал отдавать шеры — поменяй в `backend/.env` строку `APIFY_ACTOR_ID=apify/instagram-reel-scraper` (актор стоит примерно столько же, поля совместимы) и перезапусти бэкенд. Это опционально и не требуется для работы Сессии 12

---

**Сессия 13: Деплой на Vercel + Railway** (завершена 2026-04-26 — задеплоено и проверено в проде)
- **Подготовка бэкенда к Railway**:
  - `backend/railway.toml` — явная конфигурация для Nixpacks: `startCommand = "npm start"`, `healthcheckPath = "/health"` (Railway будет дёргать `/health` после деплоя и помечать сервис «живым» только после успешного ответа), `restartPolicyType = "ON_FAILURE"` с пятью попытками. Без healthcheck-пути Railway считает сервис здоровым по факту запуска процесса — мы хотим, чтобы он проверял именно HTTP-ответ
  - `backend/Procfile` (`web: npm start`) — резервный путь для Heroku-совместимых раннеров. Railway подхватывает либо `Procfile`, либо `railway.toml`, либо просто `npm start` из `package.json`. Дублирование не мешает, гарантирует устойчивость к смене подложки
  - `backend/.env.example` — комментарий про `PORT` дополнен предупреждением «не задавай вручную в Railway» (Railway сам инжектит `PORT`, ручная переменная конфликтует). К `FRONTEND_ORIGINS` добавлен пример с прод-URL Vercel
- **Подготовка фронтенда к Vercel**:
  - `frontend/.env.example` приведён в порядок: `BACKEND_URL` стал основной переменной (раньше там был `RAILWAY_BACKEND_URL`, при этом код в `backendClient.ts` читает обе — но первичной должна быть одна, иначе путаница). Комментарий объясняет различие локального (`http://127.0.0.1:3001`) и прод-значения (URL Railway)
  - Существующий fallback `process.env.RAILWAY_BACKEND_URL` в `backendClient.ts` оставил — на случай, если у Влада уже сохранена локальная env с этим именем, не хочу ломать установленное
  - `next.config.mjs` менять не пришлось — Vercel сам определяет Next.js. `tsconfig.json` и `tailwind.config.ts` тоже корректны. `dynamic = "force-dynamic"` на `/blog/references` гарантирует, что Next не пытается отрендерить страницу на этапе билда без живых env-переменных
- **DEPLOY.md** в корне репо — пошаговая инструкция для Влада: Railway сначала (Root Directory = `backend`, переменные, Generate Domain), потом Vercel (Root Directory = `frontend`, переменные, Deploy), потом замыкание CORS через `FRONTEND_ORIGINS`. С предупреждениями про типичные грабли (service role vs anon, `127.0.0.1` vs `localhost`, отсутствие `PORT` в Railway-переменных)

Решения, принятые намеренно:
- **Не пишу `vercel.json` в корне.** Vercel хорошо работает с монорепо через UI-настройку Root Directory; конфиг-файл нужен только для нестандартных кейсов (ребил build-команды, кастомные routes, env per-env). У нас ничего такого нет — лишний файл = лишняя поверхность для багов
- **`railway.toml` положен внутри `backend/`, а не в корне.** Так Railway увидит конфиг, только когда Root Directory = `backend`. Если положить в корень — Railway применит его и к попыткам сборки фронта, чего мы не хотим
- **Деплой запускает Влад сам.** Я подготовил конфиги и инструкцию, но не дёргаю Vercel/Railway API из CLI — нужны его аккаунты и токены, и любая ошибка авторизации = потерянное время. Гайд писан так, чтобы пройти его за один присест без обращения ко мне

Финальные URL прода (зафиксированы 2026-04-26):
- **Фронт (Vercel)**: https://potok-omega.vercel.app — открывается с любого устройства, на главной редирект на `/blog/references`
- **Бэкенд (Railway)**: https://potok-production-a2dc.up.railway.app — health-чек: `/health`. В переменной `BACKEND_URL` на Vercel прописан этот URL (без слэша в конце)
- **CORS**: на Railway в `FRONTEND_ORIGINS` добавлен `https://potok-omega.vercel.app` (плюс `http://localhost:3000` для локальной разработки против прод-бэкенда)
- **Авто-деплой**: каждый push в `main` триггерит и Vercel, и Railway. Если хочется откатить — Vercel и Railway держат историю деплоев, можно одной кнопкой вернуться на предыдущий

Ручные шаги для Влада на будущее: см. [DEPLOY.md](DEPLOY.md) — там пошаговая инструкция, если придётся пересоздавать проект или менять переменные.

---

**Сессия 14: Таблица закладок + поле оценки** (завершена 2026-04-26)
- Миграция `supabase/migrations/0007_bookmarks_and_rating.sql`:
  - В `videos` добавлен столбец `rating` (`text` с CHECK на `'verified' | 'super' | 'repeat'`, дефолт `null`). Частичный индекс `videos_rating_idx where rating is not null` — фильтрация по оценке быстрая, не оценённые видео индекс не раздувают
  - Новая таблица `bookmarks` — структура повторяет основные поля `videos`: url (unique), published_at, author/author_url, caption, thumbnail_url, статистика (views/likes/comments/shares/virality_score), AI-поля (ai_summary, transcript, ai_category, ai_category_suggestion) и `user_note`. **Не связана с videos через FK** — это отдельная сущность «не для блога, но не хочу терять». Триггер `bookmarks_set_updated_at`. RLS открытая, как у остальных таблиц
  - Категории/теги/оценка в bookmarks **не дублируются** — это сущности блога, в закладках не нужны
- Новый тип `Rating = "verified" | "super" | "repeat"` в `frontend/src/lib/types.ts`. Поле `rating: Rating | null` добавлено в интерфейс `Video`. Маппинг в `videosService.ts` читает столбец `rating`
- `frontend/src/lib/rating.ts` — единый конфиг: `RATINGS` (slug → `{ emoji, label }`) + `RATING_ORDER`. Меняешь подпись или эмодзи здесь — обновляется и поповер ячейки, и фильтр в шапке
- `frontend/src/lib/manualFieldsService.ts`:
  - `setVideoRating(videoId, rating)` — браузерная мутация, как `setVideoMyCategory`
  - `moveToBookmarks(videoId)` — select полей видео → upsert в bookmarks (`onConflict: "url", ignoreDuplicates: true`) → delete из videos. Транзакций между таблицами в supabase-js из браузера нет; порядок «insert до delete» гарантирует, что данные не потеряются, а `ignoreDuplicates` защищает от случайного двойного переноса. Используется не сейчас — это фундамент под Tinder-режим (Сессия 15)
- Компоненты:
  - `frontend/src/components/blog/analyst/RatingCell.tsx` — кнопка-эмодзи с поповером. Если оценка стоит — кнопка с фоном `bg-elevated`, эмодзи всегда видна. Если оценки нет — иконка-звёздочка `Star`, видна только при hover строки (`group-hover:opacity-100` — `tr` имеет класс `group`). Поповер: три кнопки (✅/🔥/🔄), повторный клик по выбранной снимает оценку, либо «Снять оценку» снизу. `position: fixed` через `getBoundingClientRect` + `createPortal` в `document.body` — не клипается `overflow:hidden` таблицы, закрывается по Esc/клику вне/скроллу — копия паттерна из `EntitySelectPopover`
  - `ThumbnailCell.tsx` — теперь принимает `videoId`, `rating`, `onSelectRating` и рисует превью + RatingCell в `flex gap-2`. Min-width столбца «Превью» поднят с 84px до 120px, чтобы вместить и картинку, и кнопку оценки
  - `VideoTableRow.tsx` — в `VideoTableRowCallbacks` добавлен `onSelectRating`, прокинут до ячейки превью
  - `AnalystWorkspace.tsx` — `handleSelectRating` повторяет паттерн `handleSelectMyCategory`: оптимистичное обновление state → запрос в Supabase → alert при ошибке
- Фильтр по оценке:
  - `frontend/src/lib/videoFilters.ts` — тип `RatingFilter = "any" | "none" | Rating`, поле `rating` в `FilterState`, чистая `matchesRating()`. Учтено в `isDirty` и в `applyFilters`
  - `FilterBar.tsx` — новый чип «Оценка»: select с пунктами «Любая», «Не оценено», «✅ Верифицировано», «🔥 Супер», «🔄 Повторить». Список оценок и эмодзи берутся из `RATING_ORDER`/`RATINGS`

Решения, принятые намеренно:
- **Оценка хранится в `videos`, а не в отдельной таблице.** Каждое видео имеет максимум одну оценку — связь 1:1 не нуждается в join-таблице (в отличие от тегов, где many-to-many)
- **`rating` — `text` с CHECK, а не Postgres `enum`-тип.** Расширять enum миграциями неудобно; CHECK легче править — только альтер таблицы. Меньше текущих и будущих ошибок
- **bookmarks не связан FK с videos.** Перенос — это «вырезать и вставить»: после `moveToBookmarks` исходной строки в videos нет, FK был бы лишней связью
- **Фронтовый `moveToBookmarks` сейчас не вызывается из UI.** Появится в Сессии 15 (Tinder-режим, свайп влево → «В закладки»). Здесь — только подготовка слоя данных

Ручные шаги для Влада (после pull):
1. **Миграция Supabase**: Dashboard → SQL Editor → вставить целиком `supabase/migrations/0007_bookmarks_and_rating.sql` → Run. Миграция ничего не дропает: добавляет столбец `rating` в `videos` и создаёт новую таблицу `bookmarks`
2. Перезапускать ничего не нужно — это правки только во фронте. Если фронт уже запущен, изменения подтянутся hot reload-ом, иначе `& "C:\Program Files\nodejs\npm.cmd" run dev` в `frontend/`
3. Открой страницу «База референсов». Наведи мышь на любое видео — в ячейке превью справа от картинки появится иконка-звёздочка. Кликни → поповер с тремя оценками (✅ Верифицировано / 🔥 Супер / 🔄 Повторить). Выбери любую — рядом с превью встанет соответствующее эмодзи
4. В фильтрах сверху появился новый чип «Оценка» — выбери «✅ Верифицировано» или «Не оценено», таблица отфильтруется

---

**Сессия 15: Tinder-режим — каркас и свайп-механика** (завершена 2026-04-26)
- **Без `react-tinder-card`** — спецификация разрешала «или аналог», и я выбрал самописную реализацию через нативные pointer events. Причины: `react-tinder-card` (~2 года без обновлений, peerDeps на React 16/17) — риск несовместимости с React 19, который мы используем; и наш use case проще «колоды Tinder» — одна карточка за раз, своё мини-меню после свайпа влево, специфичные клавиши. Самописный код — ~330 строк в одном файле, меньше чем поверхность багов от внешней либы
- `frontend/src/lib/instagramEmbed.ts` — `extractInstagramShortcode(url)` достаёт shortcode из ссылок `/reel/`, `/reels/` и `/p/`. `buildInstagramEmbedUrl(url)` собирает `https://www.instagram.com/reel/<shortcode>/embed/` (минималистичный плеер без рекомендаций и панелей профиля). Регекс синхронизирован с `reelsUrlParser.ts`
- `frontend/src/components/blog/analyst/SwipeViewer.tsx` — полноэкранный режим (`fixed inset-0 z-[100]` через `createPortal` в body, чтобы не клипалось ничем выше по дереву):
  - **Снапшот списка**. Принимает `videos: Video[]` и фиксирует его через `useState(initialVideos)` на момент открытия. Удаления и переносы в закладки меняют parent state, но мы продолжаем итерацию по снимку — иначе индекс «прыгал» бы при каждой мутации
  - **Управление состоянием карточки**: `index` (текущая позиция), `drag: {dx, dy}` (активное перетаскивание), `exitDir: "left" | "right" | null` (карточка анимированно уезжает), `postLeftMenu: boolean` (после левого свайпа показано мини-меню), `busy` (пока async-операция). Каждое изменение `index` сбрасывает остальное через useEffect
  - **Pointer events** на самой карточке: `pointerdown` ловит начало (`setPointerCapture`, чтобы курсор за пределами не терялся), `pointermove` обновляет `drag`, `pointerup` решает: если `|dx| > 110px` или скорость >0.6 px/ms — фиксирует свайп, иначе снэп назад. Iframe исключён из перехвата (`if target.tagName === "IFRAME" return`) — чтобы Instagram-плеер получал свои клики; свайп работает по бортам карточки и инфо-панели снизу
  - **CSS-анимация ухода**: `transform: translateX(±1500px) rotate(±30deg); opacity: 0` с `transition: 250ms ease`. После `setTimeout(250)` — либо открываем `postLeftMenu`, либо инкрементим `index`
  - **Клавиатура**: глобальный `keydown`-listener — `←`/`→` триггерит свайп (с `e.preventDefault()`, чтобы не скроллить страницу), `Esc` закрывает viewer (или скрывает меню, если оно открыто)
  - **Карточка**: iframe с embed-URL внутри `flex-1` контейнера; `referrerPolicy="no-referrer"` обязателен (Instagram режет с чужим Referer). Если shortcode не извлечь — fallback на превью через `/api/thumbnail` + кнопка «Открыть в Instagram» по центру. Если ни URL, ни превью — короткое сообщение
  - **Инфо-панель** под видео: автор (uppercase, faint), AI-саммари (`line-clamp-2`), категория-pill через существующий `EntityChip` (цвет blue). Для категории `other` с подсказкой выводим как «Другое: <подсказка>» через готовый `formatAiCategory()`
  - **Layout**: header (close + счётчик «N из M» + spacer для центровки) → область карточки (карточка по центру + chevron-кнопки L/R справа и слева, скрыты на мобиле) → подсказка управления внизу
  - **Мини-меню после свайпа влево**: оверлей `bg-ink/60 backdrop-blur-sm` с диалогом, три кнопки — «🗑 Удалить» (красная), «📌 В закладки» (нейтральная), «Отмена» (текст). Выбор вызывает соответствующий коллбэк, при ошибке — alert и остаёмся
  - **Завершение списка**: когда `index >= total` — отдельный экран «Все видео просмотрены» с кнопкой возврата
- `AnalystWorkspace.tsx`:
  - State `viewerSnapshot: Video[] | null` — null = viewer закрыт
  - Кнопка «Режим просмотра» (`PlayCircle` icon) в тулбаре над таблицей рядом с fullscreen-кнопкой. Disabled при `filtered.length === 0`. Подпись «Режим просмотра» скрыта на мобиле (`hidden sm:inline`), остаётся только иконка
  - Открытие: `setViewerSnapshot(filtered)` — даём snapshot текущего отфильтрованного списка
  - `handleViewerDelete` и `handleViewerBookmark` — оптимистично режут видео из workspace state, при ошибке откатываются. Удаление в режиме просмотра идёт **без** ConfirmDialog (мини-меню само по себе подтверждение, диалог в диалоге — лишний клик); перенос в закладки использует существующий `svc.moveToBookmarks` (был готов с Сессии 14)

Решения, принятые намеренно:
- **Pointer events, не `react-tinder-card`.** Собственная реализация даёт контроль над поведением (мини-меню после свайпа, исключение iframe из drag-зоны), уменьшает количество зависимостей и не упирается в проблемы совместимости React 19. Не критично «иметь либу» — критично иметь нужное поведение
- **Снапшот списка на момент открытия.** Альтернатива — переживать live-обновления: но тогда удаление текущей карточки сразу подвинуло бы индекс и пользователь увидел бы скачок. Снимок гарантирует линейный обход
- **Iframe не перехватываем для свайпа.** Tinder-карточка с интерактивным контентом — вечный конфликт. Компромисс: свайп работает по бортам и инфо-панели, плюс есть видимые chevron-кнопки на десктопе и клавиатура. Мобильным — те же кнопки + жесты по бортам карточки (узкое место для UX, посмотрим как Влад пользуется и подкрутим если что)
- **Удаление без второго подтверждения.** Мини-меню «Удалить / В закладки / Отмена» уже выполняет роль подтверждения — навешивать сверху ConfirmDialog бессмысленно
- **Свайп вправо в этой сессии — просто переход.** Воронка с категорией/заметкой/тегами — это Сессия 16. Сейчас правый свайп листает дальше, не теряя данные
- **Кнопка в тулбаре workspace, не в шапке страницы.** Спецификация писала «рядом с Добавить видео», но кнопке нужно знать `filtered.length` (для disabled), а это live state клиентского workspace. Класть state-зависимую логику в server-компонент через прокидывание — неоправданно сложно. В тулбаре рядом с fullscreen — естественное место для «view-controls»

Известные ограничения сессии 15:
- Свайп жестом по самому видео не работает (iframe перехватывает) — пользоваться чевронами / клавишами / свайпом по бортам
- Если iframe не загрузился (Instagram отдал ошибку), мы не детектим это — пользователь увидит пустой плеер. Делать таймаут с переключением на fallback — отдельная полировка, оставил на будущее
- Мини-меню после свайпа влево показывает «Удалить / В закладки» — без возможности «передумать и оставить». Возврат — кнопка «Отмена» в меню (или Esc), которая прячет меню; чтобы вернуться к карточке, нужно нажать «Отмена», иначе уже после анимации карточка исчезла. По факту — кликни «Отмена» и листай правым свайпом дальше

Ручные шаги для Влада (после pull):
1. **Перезапускать ничего не нужно** — это чисто фронтовая сессия без новых зависимостей и без миграций. Hot-reload Next.js подтянет всё. Если фронт не запущен — `& "C:\Program Files\nodejs\npm.cmd" run dev` в `frontend/`
2. На странице «База референсов» в тулбаре над таблицей появилась кнопка «Режим просмотра» (иконка плеера) — рядом с кнопкой развернуть/свернуть
3. Кликни → откроется полноэкранный режим. По центру — карточка с Instagram-плеером и инфо-панелью под ним (автор, саммари AI, категория). В шапке — счётчик «1 из N» и крестик
4. Управление: клавиши `←` (мимо) / `→` (следующее), `Esc` — закрыть. На десктопе — крупные стрелки по бокам экрана. Перетягивание мышью по бортам карточки или инфо-панели — тоже свайп
5. После свайпа влево карточка улетает и появляется мини-меню «Удалить / В закладки / Отмена»
6. Свайп вправо — просто переход к следующему видео (полноценная воронка с категорией/заметкой/оценкой будет в следующей сессии)
7. Когда видео закончатся — экран «Все видео просмотрены» с кнопкой возврата к таблице

---

**Сессия 16: Tinder-режим — воронка правого свайпа** (завершена 2026-04-27)
- Новый сервис `frontend/src/lib/manualFieldsService.ts` → `saveSwipeRightFlow(videoId, payload)`:
  - Тип `SwipeRightPayload` с полями `myCategoryId?`, `note?`, `rating?`, `tagIdsToAttach?`. **Все поля опциональные** — `undefined` означает «пользователь пропустил шаг», в апдейт колонка не идёт. `null` — это явное «снять значение», уходит в БД как есть. Это нужно, чтобы пропуск шага не затирал ранее проставленную категорию/оценку
  - Реализация: один `update` по `videos` (только заполненные поля) + опциональный `upsert` в `video_tags` пачкой (с `ignoreDuplicates: true`). Минимум round-trip'ов; снять тег здесь нельзя — это «быстрый разбор», не редактор
- Новый компонент `frontend/src/components/blog/analyst/SwipeRightFlow.tsx` (~370 строк, делится на под-компоненты `Header` / `CategoryStep` / `NoteTagsStep` / `RatingStep`):
  - **Оверлей**: `absolute inset-0 z-[110]` поверх `SwipeViewer` (он уже сидит в портале с `z-[100]`), с `bg-ink/70 backdrop-blur-sm`. На мобильных — `items-stretch` + `self-end`, чтобы диалог прилипал к низу экрана и был достижим большим пальцем; на ≥sm — центруется
  - **Состояние** копится локально и не пишется в БД до самого конца: `myCategoryId`, `note`, `tagIds`, `rating`. **Rating стартует с `"verified"`** — свайп вправо = «одобрено», это семантика жеста, явно проговорённая в спеке
  - **Шаг 1 — Категория**: AI-подсказка с иконкой `Sparkles`, инлайн-список с фильтр-инпутом и опцией «Создать категорию» (если введено имя без точного совпадения). Выбор/создание автоматически листает на шаг 2. **Не использую `EntitySelectPopover`** — поповер требует якорь, а внутри модального флоу нужен инлайн-список без двойного клика. Логика поиска/создания скопирована из поповера
  - **Шаг 2 — Заметка + теги**: textarea с автофокусом (`Ctrl/Cmd+Enter` → быстрый Next), под ней ряд тег-pill'ов (`aria-pressed`, цветные при активном состоянии, нейтральные — когда нет). Снизу — мини-инпут «Новый тег…» + кнопка «Создать»: позволяет накинуть свежий тег без выхода из флоу. Кнопки «Пропустить» (текстовая ссылка) и «Далее» (акцентная)
  - **Шаг 3 — Оценка**: grid из трёх крупных кнопок-карточек (`grid-cols-3 gap-2`) — эмодзи + подпись. Активная — `border-accent bg-accent-soft shadow-card`. Повторный клик по выбранной снимает оценку (`null`). Подсказка «свайп вправо = одобрено по умолчанию», кнопки «Пропустить» (сохраняет с `rating: "verified"`) и «Готово» (сохраняет state как есть)
  - **Esc** на любом шаге = `onCancel` (закрыть без сохранения, карточка остаётся на месте)
- Изменения в `SwipeViewer.tsx`:
  - Новые пропы: `myCategories`, `tags`, `onCreateMyCategory`, `onCreateTag`, `onSubmitRightFlow`
  - State `rightFlowOpen: boolean`
  - **Правый свайп НЕ запускает анимацию ухода**: `triggerSwipe("right")` теперь только сбрасывает drag и ставит `rightFlowOpen=true`. Карточка остаётся на месте — видна за полупрозрачным `bg-ink/70` подложки воронки. Это явное требование спеки «видео остаётся видно на фоне, затемнённое»
  - Глобальный keydown в SwipeViewer теперь рано выходит при `rightFlowOpen` — стрелки не листают карточку поверх открытой воронки. Esc обрабатывается уже самой воронкой (свой listener в useEffect)
  - Pointer-handlers тоже игнорируют события при `rightFlowOpen` — даже если как-то проскочит к карточке (фактически воронка перекрывает её)
  - На успешный submit: `setRightFlowOpen(false)` + `setIndex(i+1)` — переход к следующей карточке. На cancel: только `setRightFlowOpen(false)`, индекс не трогаем
- Изменения в `AnalystWorkspace.tsx`:
  - `handleViewerSubmitRightFlow(videoId, payload)` — оптимистичное обновление локального state видео (накладывает только заполненные поля, мерджит `tagIds` через Set), потом запрос в Supabase через `svc.saveSwipeRightFlow`. На ошибку — откат по snapshot и пробрасывает throw наверх
  - В `<SwipeViewer>` пробрасываются `myCategories`, `tags`, `handleCreateMyCategory`, `handleCreateTag`, `handleViewerSubmitRightFlow`. Существующие `handleCreate*` уже умеют добавлять созданную сущность в state — переиспользуем их

Решения, принятые намеренно:
- **Без анимации ухода на правом свайпе.** Альтернатива — анимировать карточку в сторону, потом показать модалку. Но спека прямо говорит: видео должно быть видно за затемнённой подложкой. Это естественно для «приостановки на воронке» — пользователь видит, что ещё работает с тем же видео, не успел потерять контекст
- **Без `EntitySelectPopover` в флоу.** Поповер требует якорь и открывается по клику — для шага «выбери категорию» это лишний клик. Инлайн-список с тем же поведением (поиск + создание + клик-выбор) даёт single-click flow
- **Сохранение одним батчем в самом конце, не пошагово.** Альтернатива — сохранять каждый шаг отдельно. Это чище для отмены: если пользователь нажмёт «Закрыть» на любом шаге, в БД ничего не записано. Один atomicity-юнит на флоу
- **Создание тегов внутри флоу разрешено.** Спека буквально пишет «ряд тег-кнопок (все существующие теги)», но на практике пользователь часто хочет добавить новый ярлык на лету. Мини-инпут «Новый тег» снизу — минимально навязчивый аффорданс, использовать необязательно
- **«Пропустить» на шаге оценки сохраняет verified.** «Пропустить» — это «не хочу думать про градацию», а свайп вправо уже выразил «одобрено». Без этого пользователь, который хочет быстро пройти весь флоу одним пропуском, не получит даже базовой оценки

Известные ограничения сессии 16:
- Если запрос упадёт прямо во время сохранения — alert с ошибкой, карточка не листается, локальный state откатывается. Пользователь может закрыть воронку и попробовать в табличном режиме. Retry прямо из воронки не предусмотрел — для пятисекундного флоу это лишний клик
- В шаге 2 при создании тега новый тег попадает в state `tags` (через `handleCreateTag` в workspace) и автоматически выбирается. Если параллельно открыта другая вкладка с тем же приложением — там новый тег появится только при следующем revalidatePath, не realtime

Ручные шаги для Влада (после pull):
1. **Перезапускать ничего не нужно** — это чисто фронтовая сессия без новых зависимостей и без миграций. Hot-reload Next.js подтянет всё. Если фронт не запущен — `& "C:\Program Files\nodejs\npm.cmd" run dev` в `frontend/`
2. На странице «База референсов» открой «Режим просмотра», свайпни карточку **вправо** (мышью / клавишей `→` / drag по бортам) — поверх неё откроется воронка
3. **Шаг 1 (Категория)**: сверху подсказка «AI считает: <категория>». Кликни на нужную категорию или введи новое имя и Enter — автоматический переход на шаг 2. Кнопка «Пропустить →» снизу справа
4. **Шаг 2 (Заметка + теги)**: введи короткую заметку, отметь теги в ряду pill'ов (можно несколько), при необходимости создай новый тег в инпуте «Новый тег…» — он сразу подсветится. `Ctrl+Enter` или кнопка «Далее» — на шаг 3
5. **Шаг 3 (Оценка)**: по умолчанию подсвечено ✅ Верифицировано. Можно переключить на 🔥 Супер или 🔄 Повторить, или снять оценку повторным нажатием. «Готово» → сохраняет всё одним батчем и листает на следующую карточку. «Пропустить» сохранит как «верифицировано»
6. На любом шаге `Esc` или «Закрыть» в шапке диалога = выйти без сохранения, карточка останется на месте

---

**Сессия 16.1: Multi-select для оценок** (завершена 2026-04-27)
- **Исправляем поведение из Сессии 16**: оценка перестаёт быть single-select, теперь это массив. Влад может одновременно отметить, например, ✅ и 🔥 — это два разных аспекта (полезно vs выдающееся), и хранить только одно — потеря смысла
- Миграция `supabase/migrations/0008_ratings_multiselect.sql`:
  - Добавляет колонку `videos.ratings text[] not null default '{}'`
  - Переносит существующие значения: `update set ratings = array[rating] where rating is not null` (старые одиночные оценки превращаются в массив из одного элемента — данные не теряются)
  - CHECK `ratings <@ array['verified','super','repeat']` — каждое значение из допустимого набора, оператор `<@` это «содержится в»
  - GIN-индекс по массиву (`using gin (ratings)`) — быстрый фильтр «оценки содержат X»
  - Удаляет старую колонку `rating` (вместе с её индексом и check-constraint автоматически)
- Тип `Video.rating: Rating | null` → `Video.ratings: Rating[]`. Пустой массив = «не оценено». Параллельно в `videosService.ts` маппинг строки БД использует `isRating()` type-guard — если в БД случайно попадёт мусор (например, после ручного вмешательства), отфильтруется молча
- `manualFieldsService`: `setVideoRating(id, rating)` → `setVideoRatings(id, ratings)`. `SwipeRightPayload.rating?: Rating | null` → `SwipeRightPayload.ratings?: Rating[]`. Семантика остаётся: `undefined` — не трогать, заполненный массив — записать
- `videoFilters.matchesRating`: было `v.rating === filter`, стало `v.ratings.includes(filter)`. Фильтр «Не оценено» — `v.ratings.length === 0`. UI фильтра в `FilterBar` без изменений (тип `RatingFilter` тот же)
- `RatingCell.tsx` переписан в multi-select:
  - Кнопка-якорь показывает все эмодзи в ряд (был один). При двух-трёх оценках кнопка чуть шире (`px-1.5` вместо квадратной)
  - Поповер: каждый пункт — toggle (`aria-pressed`), галочка для активных. **Поповер не закрывается на toggle** — можно отметить сразу два-три значения одним заходом, как в `TagsCell`
  - Кнопка «Снять все оценки» снизу, активна только когда есть хоть что-то
- `SwipeRightFlow.RatingStep` тоже multi-select: state `ratings: Rating[]` стартует с `["verified"]` (свайп вправо = одобрено по умолчанию), кнопки ✅/🔥/🔄 работают как toggle. «Готово» сохраняет state как есть; «Пропустить» — с дефолтом `["verified"]`. Текст подсказки переписан под новое поведение
- Имена коллбэков переименованы для ясности: `onSelectRating` → `onUpdateRatings` (`AnalystWorkspace`, `VideoTableRowCallbacks`, `ThumbnailCell`). Меняли семантику — лучше менять и имя, чтобы старое значение больше не путало

Решения, принятые намеренно:
- **`text[]` в БД, не три boolean-колонки.** Массив гибче (если вдруг добавим ✨ или ⭐ — миграция дешевле, в отличие от трёх отдельных полей). CHECK ограничивает значения текущим набором
- **Не пишем `null` ни в payload, ни в БД.** Семантика «не оценено» = пустой массив. Это снимает вопрос «это `null` потому что юзер не трогал, или потому что снял?» — `undefined` означает «не трогали», `[]` — «снял всё». Чище для diff'ов и для отладки
- **GIN-индекс без частичного `WHERE`.** Прошлый `videos_rating_idx` был частичным (`WHERE rating IS NOT NULL`) — разумно для одиночного значения. Для массива нет смысла исключать пустые: GIN сам умеет с этим работать, частичный индекс усложнит query plan'ы
- **Существующие записи мигрируют автоматически.** Те, у кого `rating` стоял — получат массив из одного элемента. Те, у кого `null` — получат пустой массив. Никаких ручных правок Влад делать не должен

Ручные шаги для Влада (после pull):
1. **Миграция Supabase**: Dashboard → SQL Editor → вставить целиком `supabase/migrations/0008_ratings_multiselect.sql` → Run. ⚠️ **Эта миграция дропает колонку `rating`** в `videos` (заменяя её на `ratings`). Существующие оценки переносятся автоматически в первый шаг up-перед drop, **но если ты вручную писал что-то нестандартное в `rating` через Dashboard — сделай бэкап**. Стандартные значения (`verified`/`super`/`repeat`/`null`) переносятся без потерь
2. После миграции Vercel автоматически задеплоит фронт по push'у в `main` (никаких env не меняется)
3. На сайте: открой видео → нажми на эмодзи рядом с превью → можешь отметить любую комбинацию из ✅/🔥/🔄. Все отмеченные эмодзи будут отображаться в ячейке
4. В режиме просмотра свайп вправо → шаг 3 «Оценка»: ✅ Верифицировано подсвечено по умолчанию, можешь дополнительно ткнуть 🔥 и/или 🔄. «Готово» сохраняет всё, «Пропустить» оставит ✅
5. Фильтр «Оценка» в верхней панели: «✅ Верифицировано» теперь означает «среди оценок этого видео есть verified» (не «единственная оценка — verified»). То есть видео с ✅+🔥 попадёт и под фильтр «Верифицировано», и под фильтр «Супер»

---

### 🔄 Текущая задача
*(нет — Сессия 16.1 завершена, следующая — Сессия 17: голосовые заметки)*

### 📋 Следующие задачи

---

**Сессия 17: Голосовые заметки**
Цель: возможность наговорить заметку голосом вместо набора текста.

Что сделать:
- **Компонент `VoiceNoteButton.tsx`** — кнопка-микрофон (Mic из lucide-react). Размещается:
  - В `SwipeRightFlow.tsx` — рядом с полем заметки (шаг 2)
  - В `NoteCell.tsx` — рядом с textarea в поповере заметки в таблице
- **Механика записи**: использовать `MediaRecorder` API браузера.
  - Нажал и держишь (или нажал-отпустил для toggle) → запись идёт, кнопка пульсирует красным, показывается таймер "0:03..."
  - Отпустил (или нажал повторно) → запись останавливается → аудио отправляется на бэкенд
- **Бэкенд — новый эндпоинт `POST /api/voice-to-text`**:
  - Принимает аудиофайл (webm/mp4)
  - Отправляет в OpenAI Whisper (тот же сервис, что и для транскрипции видео)
  - Возвращает `{ text: "расшифрованный текст", suggestedTags: ["tag_id_1", "tag_id_2"] }`
  - **Определение тегов**: после Whisper отправляет текст заметки в GPT с промптом: "Вот заметка пользователя о видео: [текст]. Вот список доступных тегов: [список name + описание]. Выбери 0–3 подходящих тега. Верни JSON массив tag_id. Если ни один не подходит — пустой массив. НЕ придумывай новые теги." Модель: `gpt-4o-mini`, temperature 0.2
- **Фронтенд**: после получения ответа — текст вставляется в поле заметки (пользователь может отредактировать), предложенные теги автоматически выбираются (но пользователь может снять)
- **Разрешение микрофона**: при первом нажатии браузер запросит доступ. Если отказал — показать сообщение "Разрешите доступ к микрофону в настройках браузера"

---

### 🔜 Отложено (v3+)
- Скриншоты к заметкам (хранение на Google Drive) — когда станет понятно что без них не обойтись
- Раздел "Закладки" на сайте с полноценным интерфейсом (пока доступ через Supabase Dashboard)
- Парсинг целого профиля блогера (все видео + общая статистика аккаунта)
- Превью: скачивание в Supabase Storage для постоянных URL (сейчас временные ссылки Instagram CDN)
- Другие разделы суперапа: Финансы, Новости, Сохранёнки, Читалка, Второй мозг

---

## Известные ограничения и заметки
- Instagram API закрыт — используем Apify (платный, pay-as-you-go)
- Массовая обработка: 1 видео ≈ 60–120 сек (Apify+Whisper+AI). При workerConcurrency=2 пачка из 1000 займёт ~8 часов. Hard-лимит UI на пачку — 2000 ссылок (`MAX_BATCH_SIZE` в `frontend/src/lib/reelsUrlParser.ts`)
- Коэффициент вирусности: формулу уточним по ходу (соотношение лайков/комментов к просмотрам, нормированное по среднему для аккаунта)
- Транскрипция: только речь. Текст на экране (субтитры, наложенный текст) не распознаём — усложнение без большой ценности на данном этапе
- **Whisper API лимит 25 МБ.** Видео Reels часто превышают, поэтому в `transcriptionService` перед отправкой ffmpeg вытаскивает аудио в mp3 64 kbps моно — размер падает в 20–50 раз. Бинарь ffmpeg ставится пакетом `ffmpeg-static`, никаких системных установок не нужно
- **Шеры (количество отправок Reels)**: дефолтный актор `apify/instagram-scraper` поле не отдаёт. Альтернатива — `apify/instagram-reel-scraper` (специализированный, возвращает `sharesCount`); чтобы переключиться, поставить `APIFY_ACTOR_ID=apify/instagram-reel-scraper` в `backend/.env` и перезапустить бэкенд. В дефолтной конфигурации шеры остаются `null`, формула вирусности корректно работает без них
- **Превью с Instagram CDN**: ссылки временные (~24 часа) и требуют отсутствия Referer (на фронте уже стоит `referrerPolicy="no-referrer"`). Долгосрочное решение — при парсинге скачивать обложку в Supabase Storage и хранить постоянный URL. Запланировать отдельной сессией.
- Бэкенд локально — запускать через `npm start` (без `--watch`): `node --watch` может прервать обработку Apify на полпути и оставить строку в статусе `processing` навсегда. `npm run dev` оставлен для будущей доработки кода без активных задач Apify

---

## Контекст будущего развития (НЕ делать сейчас, но учитывать в архитектуре)
- Раздел "Команда" в меню: AI-агенты для блога (аналитик, редактор, креатор) — отдельная ветка разработки
- Связка База референсов → Редактор: перетаскивание идей, тем, хуков в создание своего контента
- Другие разделы суперапа: Финансы, Новости, Сохранёнки, Читалка, Второй мозг
- Общая инфраструктура: одна база Supabase, один Telegram-бот (для других разделов)

---

## Правила архитектуры кода — ОБЯЗАТЕЛЬНО

### Модульность
- **НИКОГДА** не складывать весь код в один файл. Каждый компонент, каждый сервис, каждая утилита — отдельный файл.
- Один React-компонент = один файл. Максимум 150–200 строк на файл. Если больше — разбить на подкомпоненты.
- Бизнес-логика отделена от UI: компоненты не содержат прямых вызовов к API или базе. Вся логика — в `lib/` или `services/`.
- Общие UI-элементы (кнопки, модалки, инпуты) — в `components/ui/`, переиспользуются везде.

### Именование
- Файлы компонентов: PascalCase (`VideoTable.tsx`, `AddVideoModal.tsx`)
- Файлы утилит и сервисов: camelCase (`apifyService.ts`, `supabaseClient.ts`)
- Папки: kebab-case (`blog-analyst/`)

### Типизация
- TypeScript обязателен. Интерфейсы для всех структур данных (Video, Tag, Category и т.д.) — в отдельном файле `types.ts`.

---

## Правила для Claude Code

### Как работать с этим файлом
- Читай CLAUDE.md в начале каждой сессии — это твой контекст
- После выполнения задачи — обнови секцию "Готово" и "Текущая задача"
- Если возникли новые ограничения или баги — добавь в "Известные ограничения"
- Если изменилась структура папок — обнови секцию "Структура папок"

### Как писать код
- Следуй правилам архитектуры выше — модульность критична
- Каждый файл — одна ответственность. Не смешивай
- Комментарии в коде — на русском, коротко, только где неочевидно
- После создания файлов — проверь что проект запускается (`npm run dev`)

### Как общаться с Владом
- Влад не разработчик. Не используй технический жаргон без объяснения
- Если нужно что-то настроить вручную (сервис, переменная окружения) — дай пошаговую инструкцию со скриншотами или точными действиями
- Если задача неясна или требует продуктового решения — скажи "Обсуди это в чате с Claude" (имеется в виду чат на claude.ai, где Влад обсуждает продукт)
